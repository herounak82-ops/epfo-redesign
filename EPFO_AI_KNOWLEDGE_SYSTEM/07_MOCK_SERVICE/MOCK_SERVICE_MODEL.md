# Mock Service Model

`synthetic_only = true`. The mock package contains non-production identifiers and deterministic member/employment/KYC/claim/request records. It represents prototype infrastructure, not official EPFO state. There are no real transactions, claims, backend calls, or valid credentials. Current UI adapters live in `src/src/services/mockData.ts`.
