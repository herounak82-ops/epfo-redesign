# Testing Model

Run the knowledge validator first. For app work, add route/state checks, keyboard/focus checks, 200% zoom, responsive viewport checks, and synthetic-disclosure checks. Test loading, ready, action-needed, pending, error, blocked, success, and recovery states. Browser validation is not claimed unless actually performed.
