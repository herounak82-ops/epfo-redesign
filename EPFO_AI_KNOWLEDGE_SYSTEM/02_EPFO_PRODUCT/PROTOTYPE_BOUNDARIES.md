# Prototype Boundaries

The current repository contains a frontend prototype with deterministic mock data. It does not authenticate real members, call EPFO production services, submit claims, alter KYC, initiate transfers, or make statutory eligibility decisions. Any future deeper batch must preserve synthetic disclosures and explicitly document what is simulated.
