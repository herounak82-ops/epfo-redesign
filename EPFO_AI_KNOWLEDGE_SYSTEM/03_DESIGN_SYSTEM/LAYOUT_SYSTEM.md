# Layout System

Use the project’s 4px base spacing, responsive containers, readable line lengths, and progressive disclosure. Mobile is the default constraint; wider layouts may add columns only when relationships remain clear. Preserve action proximity, error visibility, and table alternatives at narrow widths.
