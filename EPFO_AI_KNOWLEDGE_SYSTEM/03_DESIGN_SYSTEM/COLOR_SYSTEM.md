# Color System

Use semantic roles from `docs/tokens.json` and the existing CSS variables rather than isolated hex values. Status must combine text, icon/shape, and color. Contrast, focus, error, warning, success, and disabled states require dedicated checks. Do not use gradients or color as decoration when it competes with service state.
