# Feedback Model

Every service operation has loading, ready, action-needed, pending, success, error, and blocked states. Errors answer what happened, why, who owns it, what the member can do now, what happens next, and when an update is expected. State labels must not rely on color alone.
